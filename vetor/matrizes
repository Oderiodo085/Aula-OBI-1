#include <iostream>
#include <vector>

using namespace std;

int main (void){
    int N, M;

    cin >> n >> M;

    int D[n][3];

    for(int i = 0; i < N; i++){
        for(int j = 0; j < 3; j++){
            cin >> D[i][j];
        }
    }

    for(int i = 0; i < N; i++){
        for(int j = 0; j < 3)
    }
}